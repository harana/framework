- name: Lookup IP Address Location
  class: lookup
  inputs:
    ip: string #required
  outputs:
    city: string
    country: string
    latitude: float
    longitude: float
    region: string
    timezone: string

- name: Calculate Distance Between Coordinates
  class: distance
  inputs:
    from_lat: float #required
    from_lon: float #required
    to_lat: float #required
    to_lon: float #required
    unit: km | mi | m = km
  outputs:
    distance: float

- name: Geocode Address To Coordinates
  class: geocode
  inputs:
    address: string #required
  outputs:
    city: string
    country: string
    latitude: float
    longitude: float
    region: string

- name: Reverse Geocode Coordinates To Address
  class: reverse_geocode
  inputs:
    latitude: float #required
    longitude: float #required
  outputs:
    address: object
    formatted_address: string
