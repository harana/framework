- name: address
  id: [entity_id, entity_type, type]
  schema:
    - city: string? #max(200)
    - country: string? #max(100)
    - created_at: date = now()
    - entity_id: id #required
    - entity_type: string #required #max(100)
    - is_primary: boolean = false
    - is_verified: boolean = false
    - label: string? #max(100)
    - postal_code: string? #max(20)
    - state: string? #max(200)
    - street: string? #max(500)
    - type: home | work | shipping | billing | other = home
    - updated_at: date = now()
    - verified_at: date?

- name: address_validation_log
  id: [address_id, validated_at]
  schema:
    - address_id: id #required -> address.id
    - errors: string? # j_s_o_n array of errors
    - is_valid: boolean = false
    - provider: string? #max(100)
    - validated_at: date = now()

- name: address_validation_error
  schema:
    - field: string
    - code: string
    - message: string

- name: address_suggestion
  schema:
    - description: string
    - place_id: string
    - address: address
