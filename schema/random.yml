- object: random_seed
  schema:
    - algorithm: string = default #max(50)
    - created_at: date = now()
    - seed_value: int?

- object: random_value
  schema:
    - seed: int
    - value: any
    - type: string
