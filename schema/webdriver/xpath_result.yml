- name: webdriver_xpath_result
  id: [session_id, xpath, queried_at]
  schema:
    - element_count: int = 0 #min(0)
    - error_message: string? #max(2000)
    - queried_at: date = now()
    - results: string? # json array of element data
    - session_id: id #required -> web_driver_session.id
    - status: success | failed | not_found | timeout = success
    - xpath: string #required #max(4000)