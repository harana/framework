- object: cf_env_variable
  id: [worker_id, name]
  schema:
    - created_at: date = now()
    - is_secret: boolean = false
    - name: string #max(255)
    - type: plain_text | secret = plain_text
    - updated_at: date = now()
    - value: string?
    - worker_id: id -> cf_worker.id

- object: cf_secrets_store
  id: [account_id, store_name]
  schema:
    - account_id: string #max(50)
    - binding: string #max(255)
    - created_at: date = now()
    - store_name: string #unique #max(255)
    - updated_at: date = now()

- object: cf_secrets_store_secret
  id: [store_id, name]
  schema:
    - created_at: date = now()
    - name: string #max(255)
    - store_id: id -> cf_secrets_store.id
    - updated_at: date = now()

- object: cf_version_metadata
  id: [worker_id, version_id]
  schema:
    - created_at: date = now()
    - message: string? #max(500)
    - tag: string? #max(255)
    - timestamp: date?
    - version_id: string #unique #max(255)
    - worker_id: id -> cf_worker.id

