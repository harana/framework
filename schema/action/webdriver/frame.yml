- action: enter_frame
  inputs:
    frame_index: int    session_id: string  outputs:
    success: boolean

- action: enter_frame_element
  inputs:
    element_id: string    session_id: string  outputs:
    success: boolean

- action: enter_parent_frame
  inputs:
    session_id: string  outputs:
    success: boolean

- action: enter_default_frame
  inputs:
    session_id: string  outputs:
    success: boolean
