- action: get_alert_text
  inputs:
    session_id: string  outputs:
    text: string

- action: accept_alert
  inputs:
    session_id: string  outputs:
    success: boolean

- action: dismiss_alert
  inputs:
    session_id: string  outputs:
    success: boolean

- action: send_alert_text
  inputs:
    keys: string    session_id: string  outputs:
    success: boolean
