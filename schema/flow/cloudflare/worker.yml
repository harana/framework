- name: fetch
  inputs:
    body: any
    headers: headers
    method: get | post | put | patch | delete | head | options = get
    service_binding: string #required
    url: string #required
  outputs:
    body: any
    headers: headers
    status_code: int

- name: get_var
  inputs:
    name: string #required
  outputs:
    value: string

- name: get_secret
  inputs:
    name: string #required
  outputs:
    value: string

- name: scheduled
  inputs:
    cron: string #required
    service_binding: string #required
  outputs:
    scheduled_time: date
    success: boolean

- name: wait_until
  inputs:
    promise: any #required
  outputs:
    success: boolean

- name: pass_through
  inputs:
    request: any #required
  outputs:
    response: any

- name: service_binding_fetch
  inputs:
    body: any
    headers: headers
    method: get | post | put | patch | delete | head | options = get
    service: string #required
    url: string #required
  outputs:
    body: any
    headers: headers
    status_code: int

- name: get_version
  inputs:
    binding: string #required
  outputs:
    id: string
    message: string
    tag: string
    timestamp: date
