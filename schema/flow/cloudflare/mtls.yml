- name: fetch
  inputs:
    body: any
    certificate_binding: string #required
    headers: headers
    method: get | post | put | patch | delete | head | options = get
    url: string #required
  outputs:
    body: any
    headers: headers
    status_code: int
