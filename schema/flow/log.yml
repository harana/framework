- action: debug
  inputs:
    context: log_context
    message: string #required
  outputs:
    success: boolean

- action: info
  inputs:
    context: log_context
    message: string #required
  outputs:
    success: boolean

- action: warn
  inputs:
    context: log_context
    message: string #required
  outputs:
    success: boolean

- action: error
  inputs:
    context: log_context
    error: log_error
    message: string #required
  outputs:
    success: boolean

- action: structured
  inputs:
    data: log_data #required
    level: debug | info | warn | error #required
  outputs:
    success: boolean

- object: log_entry
  class:
    level: string
    message: string
    timestamp: date
    context: log_context
    error: log_error

- object: log_context
  class:
    request_id: string
    user_id: string
    trace_id: string
    span_id: string

- object: log_error
  class:
    message: string
    stack_trace: string
    code: string

- object: log_data
  class:
    key: string
    value: any
