- name: get_alert_text
  inputs:
    session_id: string #required  outputs:
    text: string

- name: accept_alert
  inputs:
    session_id: string #required  outputs:
    success: boolean

- name: dismiss_alert
  inputs:
    session_id: string #required  outputs:
    success: boolean

- name: send_alert_text
  inputs:
    keys: string #required    session_id: string #required  outputs:
    success: boolean
