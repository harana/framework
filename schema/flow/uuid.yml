- name: generate_v4
  inputs:
    count: int = 1
    uppercase: boolean = false
  outputs:
    uuid: string | list[string]

- name: generate_v7
  inputs:
    count: int = 1
    uppercase: boolean = false
  outputs:
    uuid: string | list[string]

- name: validate
  inputs:
    uuid: string #required
    version: int
  outputs:
    valid: boolean
    version: int

- name: parse
  inputs:
    uuid: string #required
  outputs:
    timestamp: int
    variant: string
    version: int

- name: uuid
  class:
    value: string
    version: int
    variant: string
    timestamp: int
