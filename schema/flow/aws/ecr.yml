- name: create_repository
  inputs:
    encryption_type: aes256 | kms
    image_tag_mutability: mutable | immutable = mutable
    kms_key: string
    region: string
    repository_name: string #required
    scan_on_push: boolean = false
    tags: ecr_tags
  outputs:
    registry_id: string
    repository_arn: string
    repository_uri: string
    success: boolean

- name: delete_repository
  inputs:
    force: boolean = false
    region: string
    repository_name: string #required
  outputs:
    success: boolean

- name: describe_repositories
  inputs:
    region: string
    repository_names: list[string]
  outputs:
    repositories: list[ecr_repository]

- name: list_images
  inputs:
    filter: image_filter
    max_results: int = 100
    region: string
    repository_name: string #required
  outputs:
    image_ids: list[image_id]
    next_token: string

- name: describe_images
  inputs:
    image_ids: list[image_id]
    region: string
    repository_name: string #required
  outputs:
    image_details: list[ecr_image_detail]

- name: put_image
  inputs:
    image_manifest: string #required
    image_tag: string #required
    region: string
    repository_name: string #required
  outputs:
    image: ecr_image
    success: boolean

- name: batch_delete_image
  inputs:
    image_ids: list[image_id] #required
    region: string
    repository_name: string #required
  outputs:
    failures: list[image_failure]
    image_ids: list[image_id]
    success: boolean

- name: get_auth_token
  inputs:
    region: string
    registry_ids: list[string]
  outputs:
    authorization_token: string
    expires_at: date
    proxy_endpoint: string
    success: boolean

- name: get_download_url
  inputs:
    image_digest: string #required
    region: string
    repository_name: string #required
  outputs:
    download_url: string
    success: boolean

- name: tag_image
  inputs:
    image_digest: string #required
    image_tag: string #required
    region: string
    repository_name: string #required
  outputs:
    success: boolean

- name: set_repository_policy
  inputs:
    force: boolean = false
    policy_text: string #required
    region: string
    repository_name: string #required
  outputs:
    policy_text: string
    registry_id: string
    repository_name: string
    success: boolean

- name: get_repository_policy
  inputs:
    region: string
    repository_name: string #required
  outputs:
    policy_text: string
    registry_id: string
    repository_name: string

- name: delete_repository_policy
  inputs:
    region: string
    repository_name: string #required
  outputs:
    success: boolean

- name: start_image_scan
  inputs:
    image_id: image_id #required
    region: string
    repository_name: string #required
  outputs:
    image_id: image_id
    scan_status: string
    success: boolean

- name: describe_scan_findings
  inputs:
    image_id: image_id #required
    max_results: int = 100
    region: string
    repository_name: string #required
  outputs:
    findings: list[image_scan_finding]
    image_scan_status: image_scan_status
    next_token: string

- name: put_lifecycle_policy
  inputs:
    lifecycle_policy_text: string #required
    region: string
    repository_name: string #required
  outputs:
    lifecycle_policy_text: string
    registry_id: string
    repository_name: string
    success: boolean

- name: get_lifecycle_policy
  inputs:
    region: string
    repository_name: string #required
  outputs:
    last_evaluated_at: date
    lifecycle_policy_text: string
    registry_id: string
    repository_name: string
