name: Combobox
type: UIComponent
id:
  - identifier

  label: String
  description: String
  placeholder: String
  aria_label: String
  default_value: String
  is_disabled: bool = false
  is_invalid: bool = false
  anchor: String enum(top, bottom) = bottom
  error_message: String
  change: event?
