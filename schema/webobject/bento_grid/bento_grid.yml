- name: bento_grid
  attributes:
    - background: white | gray = white
    - description: string? #max(500)
    - layout: mixed | three_column | two_row = mixed
    - subtitle: string? #max(200)
    - title: string? #max(200)
  events:
    - item_click
