- name: fieldset
  attributes:
    - aria_label: string? #max(200)
    - description: string? #max(500)
    - is_disabled: boolean = false
    - legend: string? #max(200)
  events:
    - disable
    - enable