- name: table_header
  id: [table_id, name]
  attributes:
    - alignment: left | center | right = left
    - label: string #required #max(200)
    - order: int = 0 #min(0)
    - table_id: id #required -> ui_table.id
  events:
    - resize
    - sort