- webobject: week_calendar
  attributes:
    - add_event_label: string = "add event" #max(50)
    - current_week_datetime: string #max(20)
    - current_week_display: string #max(50)
    - days: [calendar_day]
    - events: [calendar_event]
    - selected_view: string = "week" #max(20)
    - time_slots: [time_slot]
    - title: string #max(100)
    - today_label: string = "today" #max(20)
    - view_options: [calendar_view_option]
  events:
    - previous_week
    - next_week
    - go_to_today
    - select_view
    - select_day
    - add_event
    - view_event
    - edit_event
