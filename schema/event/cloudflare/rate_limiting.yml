- event: cloudflare_rate_limit_checked
  schema:
    rate_limiter: string #required
    key: string #required
    allowed: boolean #required
    checked_at: date = now()

- event: cloudflare_rate_limit_exceeded
  schema:
    rate_limiter: string #required
    key: string #required
    exceeded_at: date = now()
