- event: cloudflare_hyperdrive_connected
  schema:
    binding: string #required
    host: string?
    port: int?
    connected_at: date = now()

- event: cloudflare_hyperdrive_connection_failed
  schema:
    binding: string #required
    error_message: string?
    failed_at: date = now()
