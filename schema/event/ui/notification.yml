- name: notification_shown
  schema:
    notification_id: id #required
    notification_type: info | success | warning | error = info
    notification_title: string?
    notification_message: string?
    duration_ms: int? #min(0)
    shown_at: date = now()

- name: notification_dismissed
  schema:
    notification_id: id #required
    notification_type: info | success | warning | error = info
    dismiss_method: button | swipe | timeout | action = button
    time_visible_ms: int? #min(0)
    dismissed_at: date = now()

- name: notification_clicked
  schema:
    notification_id: id #required
    notification_type: info | success | warning | error = info
    notification_title: string?
    clicked_at: date = now()

- name: notification_action_clicked
  schema:
    notification_id: id #required
    action_id: id #required
    action_label: string?
    action_type: primary | secondary | dismiss = primary
    clicked_at: date = now()

- name: toast_expired
  schema:
    toast_id: id #required
    toast_type: info | success | warning | error = info
    toast_message: string?
    duration_ms: int? #min(0)
    expired_at: date = now()
