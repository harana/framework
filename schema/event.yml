
- object: event
  schema:
    - event_id: string
    - event_type: string
    - channel: string
    - payload: any
    - metadata: event_metadata
    - created_at: date

- object: event_metadata
  schema:
    - values: map[string, any]

- object: event_record
  schema:
    - event_id: string
    - event_type: string
    - channel: string
    - payload: any
    - metadata: event_metadata
    - created_at: date
    - acknowledged: boolean
